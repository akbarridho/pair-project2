<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (data) { %>
        <h1>Welcome <%= data.fullName %></h1>
    <% } else { %>
        <h1>Welcome</h1>
    <% } %>
    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>
    <% if (!data) { %>
        <button type="button" onclick="location.href='/profile/add'">Add Profile</button>
    <% } else if (data) { %>
        <button type="button" onclick="location.href='/admin'">Admin</button>
        <button type="button" onclick="location.href='/visitor'">Visitor</button>
        <button type="button" onclick="location.href='/profile/<%= data.id %>/edit'">Edit Profile</button>
    <% } %>
    <button type="button" onclick="location.href='/logout'">Log Out</button>
</body>
</html>