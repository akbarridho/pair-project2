<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
    <h1 style="text-align: center;margin-top: 20px;">Edit Book</h1>
    <% if (error) { %>
        <% error.split(",").forEach((el) => { %>
            <p style="color: red;text-align: center;"><%= el %></p>
        <% }) %>
    <% } %>
    <form method="post" action="/admin/<%= data.id %>/edit">
        <div class="mb-3" style="margin-left: 40px;margin-right: 100px;">
            <p class="form-label">Title</p>
            <input type="text" name="title" class="form-control" value="<%= data.title %>">
            <p class="form-label">Genre</p>
            <select name="genre" class="form-select">
                <option value="Horor" <%= data.genre == 'Horor' ? "selected" : "" %>>Horor</option>
                <option value="Romance" <%= data.genre == 'Romance' ? "selected" : "" %>>Romance</option>
                <option value="Fantasi" <%= data.genre == 'Fantasi' ? "selected" : "" %>>Fantasi</option>
                <option value="Romance" <%= data.genre == 'Romance' ? "selected" : "" %>>Romance</option>
                <option value="Sejarah" <%= data.genre == 'Sejarah' ? "selected" : "" %>>Sejarah</option>
            </select>
            <p class="form-label">Publish</p>
            <input type="date" name="publish" class="form-control" value="<%= formatDate(data.publish) %>">
            <p class="form-label">Description</p>
            <div class="form-floating">
                <textarea name="description" class="form-control"><%= data.description %></textarea>
            </div>
            <div>
                <button style="margin-top: 20px;" type="submit" class="btn btn-primary">Edit Book</button>
                <button style="margin-top: 20px;" onclick="location.href='/admin/<%= data.id %>'" class="btn btn-primary" type="button">Back</button>
            </div>
        </div>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>